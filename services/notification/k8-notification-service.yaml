---
apiVersion: v1
kind: ConfigMap
metadata:
  name: notification-service-configuration
  namespace: e-commerce
data:
  application.yml: |
    spring:
      config:
        import: optional:configserver:http://config-service-svc:8888
      application:
        name: notification-service
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service-svc
  namespace: e-commerce
  labels:
    app: notification-service-svc
spec:
  type: ClusterIP
  ports:
    - name: notif-port
      port: 8040
      targetPort: 8040
      protocol: TCP
  selector:
    app: notification-service-pod
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service-dep
  namespace: e-commerce
  labels:
    app: notification-service-dep
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-service-pod
  template:
    metadata:
      labels:
        app: notification-service-pod
    spec:
      containers:
        - name: notification-service-container
          image: notification-service
          imagePullPolicy: IfNotPresent
          volumeMounts:
            - name: config
              mountPath: /config/application.yml
              subPath: application.yml
          ports:
            - containerPort: 8040
              name: notif-port
          env:
            - name: SPRING_CONFIG_ADDITIONAL_LOCATION
              value: "file:/config/"
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8040
            initialDelaySeconds: 60
            periodSeconds: 10
            timeoutSeconds: 5
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8040
            initialDelaySeconds: 90
            periodSeconds: 15
            timeoutSeconds: 5
      volumes:
        - name: config
          configMap:
            name: notification-service-configuration
